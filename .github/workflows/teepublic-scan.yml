name: Weekly TeePublic Scan
on:
  schedule:
    - cron: '0 14 * * 1'
  workflow_dispatch:
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: echo hello
